use std::{sync::Arc, thread::sleep};
use tokio::time::{self, Duration};
use anyhow::Result;


pub async fn run_automation(config: ) -> Result<()> {
    loop {
        


        sleep(Duration::from_secs(30)).await;
    }
}